{
  "name": "lichtara-site",
  "private": true,
  "scripts": {
    "dev": "npx live-server pages --port=4173",
    "build": "mkdir -p dist && cp -r pages assets dist/ && cp pages/contrato-do-sim.html dist/ && [ -f pages/index.html ] && cp pages/index.html dist/index.html || true && [ -d docs ] && cp -r docs dist/ || true && [ -f governanca.html ] && cp governanca.html dist/ || true && [ -f CNAME ] && cp CNAME dist/ || true && node scripts/make-sitemap.mjs",
    "pdf": "node scripts/make-pdf.mjs",
    "check": "echo 'OK'"
  },
  "devDependencies": {
    "puppeteer": "^22.15.0"
  }
}
