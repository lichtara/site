{
  "name": "lichtara-site",
  "private": true,
  "scripts": {
    "dev": "npx live-server pages --port=4173",
    "build": "mkdir -p dist && cp -r pages assets dist/ && cp pages/contrato-do-sim.html dist/ && [ -f pages/index.html ] && cp pages/index.html dist/index.html || true && [ -d docs ] && cp -r docs dist/ || true && [ -f governanca.html ] && cp governanca.html dist/ || true && [ -f CNAME ] && cp CNAME dist/ || true && [ -f robots.txt ] && cp robots.txt dist/ || true && [ -f 404.html ] && cp 404.html dist/ || true",
    "pdf": "node scripts/make-pdf.mjs && node scripts/render-insignia.mjs",
    "md": "node scripts/md-to-html.mjs",
    "postbuild": "npm run md && node scripts/make-sitemap.mjs && node scripts/make-robots.mjs && node scripts/fix-urls.mjs",
    "check": "echo 'OK'"
  },
  "devDependencies": {
    "marked": "^12.0.2",
    "puppeteer": "^22.15.0"
  }
}
