{
  "name": "lichtara-site",
  "private": true,
  "scripts": {
    "dev": "npx live-server pages --port=4173",
    "build": "mkdir -p dist && rm -rf dist/pages || true && cp -r assets dist/ && [ -f pages/contrato-do-sim.html ] && cp pages/contrato-do-sim.html dist/ || true && [ -f pages/index.html ] && cp pages/index.html dist/index.html || true && [ -d docs ] && cp -r docs dist/ || true && [ -f pages/governanca.html ] && cp pages/governanca.html dist/governanca.html || true && [ -f pages/formacao.html ] && cp pages/formacao.html dist/formacao.html || true && [ -f pages/organizacao.html ] && cp pages/organizacao.html dist/organizacao.html || true && [ -f CNAME ] && cp CNAME dist/ || true && [ -f 404.html ] && cp 404.html dist/ || true",
    "pdf": "node scripts/make-pdf.mjs && node scripts/render-insignia.mjs",
    "md": "node scripts/md-to-html.mjs",
    "postbuild": "npm run md && node scripts/make-sitemap.mjs && node scripts/make-robots.mjs && node scripts/fix-urls.mjs",
    "check": "echo 'OK'"
  },
  "devDependencies": {
    "marked": "^12.0.2",
    "puppeteer": "^22.15.0"
  },
  "dependencies": {
    "@openai/agents": "^0.1.0"
  }
}
